# Static QR generator (URL/Wi-Fi/vCard/batch) with SVG export, no signup, no redirect

Live: https://static-qr-no-signup.sociobot.in — built by the Param Factory (`static-web`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
